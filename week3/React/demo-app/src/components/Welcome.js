// Components = sections of the page with separate functionality or elements And they are reusable

//rafce template for react- download es7+
import React from 'react'

const Welcome = ({ name }) => {
    return (
      <p>Welcome, {name}!</p>
    )
  }

export default Welcome;